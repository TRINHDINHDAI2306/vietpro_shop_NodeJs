<div class="product-item card text-center">
  <a href="/product-<%=item.slug%>.<%=item._id%>"
    ><img src="../uploads/images/<%=item.thumbnail%>"
  /></a>
  <h4>
    <a href="/product-<%=item.slug%>.<%=item._id%>"><%=item.name%></a>
  </h4>
  <p>Giá Bán: <span><%=Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(item.price).replace('₫', 'VND')%>
  </span></p>
</div>
